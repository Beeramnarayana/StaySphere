# Ensure consistent package installation
audit=false
legacy-peer-deps=true
prefer-offline=true
# Use the latest Node.js LTS version
engine-strict=true
# Skip optional dependencies
optional=false
# Disable package-lock.json to avoid conflicts
package-lock=false
